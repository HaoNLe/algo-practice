import { treeNode } from "./types";

function createBinaryTreeFromArray(treeNodes: number[]): treeNode {
  if (treeNodes.length === 0) {
    return new treeNode();
  }

  return new treeNode();
}

export default { createBinaryTreeFromArray };